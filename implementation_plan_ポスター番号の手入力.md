# 提案仕様書・実装計画：ポスター指定方法への「ポスター番号の手入力」追加

ポスター発表者へのフィードバック対象ポスターを指定する方法として、既存の**QRコードカメラ読み取り**に加え、**ポスター番号の手入力**機能を追加します。

---

## 🎯 変更の背景と目的

- **背景**: 端末やブラウザの権限設定（カメラ非許可）、会場の明るさや混雑状況によってQRコードの読み取りが難しい場合があります。
- **目的**: 参加者がポスターに掲示されたポスター番号（例: `1`, `12` など）をキーボードから直接入力してフィードバック画面に素早く遷移できるようにし、ユーザビリティと利用率を向上させます。

---

## 📐 画面仕様・UIデザイン案

### 1. トップページ ([app/page.tsx](file:///c:/Users/user/Antigravity/QRBireg/app/page.tsx))

トップページのフィードバック開始エリアを、QRコード読み取りと手入力のどちらからでも直感的に選択できるデザインに拡張します。

#### UI構成
- **入力カードエリア**:
  - **方法1: 📷 QRコードを読み取る** (従来のカメラ起動ボタン)
  - **区切り線**: 「または」
  - **方法2: 🔢 ポスター番号を直接入力**
    - 数値入力フィールド: `inputMode="numeric"`, `pattern="[0-9]*"`, `placeholder="ポスターNo.（例: 1, 12）"`
    - 「開く / フィードバック画面へ ➔」ボタン

#### 動作・バリデーションフロー
1. 参加者がポスター番号を入力して「開く」ボタンを押す（またはキーボードのEnterキーを押す）。
2. 入力値の検証:
   - 空文字または非数値の場合: 「ポスター番号を数字で入力してください」のエラーメッセージを表示。
   - 存在するポスターかの確認 (Supabase の `posters` テーブル照会):
     - ポスターが存在する場合 ➔ `/${eventId}/poster/${posterId}` へ自動遷移。
     - ポスターが存在しない場合 ➔ 「ポスターNo. XX は登録されていません。番号をご確認ください」を表示。

---

## 🛠️ 変更対象ファイル

### 1. [MODIFY] [app/page.tsx](file:///c:/Users/user/Antigravity/QRBireg/app/page.tsx)
- ポスター番号入力用ステート（`posterNumberInput`）およびエラー表示ステート（`inputError`）を追加。
- フォーム送信ハンドラー `handleManualPosterSubmit` を実装。
- Supabaseで該当ポスターの存在確認を行い、OKなら `router.push(`/${eventId}/poster/${posterId}`)` に遷移。
- QRスキャンボタンと手入力フォームが共存するスタイリッシュなUIコンポーネントを配置。

### 2. [MODIFY] マニュアル各種
- [participant_manual.md](file:///c:/Users/user/Antigravity/QRBireg/participant_manual.md): 参加者向け利用手順に「ポスター番号直接入力」でのフィードバック画面アクセス手順を追記。
- [user_manual.md](file:///c:/Users/user/Antigravity/QRBireg/user_manual.md): 操作マニュアルの記載内容を更新。

---

## 🧪 検証計画

### 1. 手入力遷移の動作確認
- トップページにて、存在するポスター番号（例: `1`）を入力して「開く」をクリック ➔ 正しく `/${eventId}/poster/1` のフィードバック画面が開くことを確認。
- Enterキー押下での送信が動作することを確認。

### 2. エラーハンドリング確認
- 空のまま送信した場合 ➔ バリデーションメッセージが表示されることを確認。
- 存在しないポスター番号（例: `999`）を入力した場合 ➔ 「ポスターNo. 999 は登録されていません」のエラーメッセージが表示され、無効な画面への遷移を防げることを確認。

### 3. モバイル表示・テンキー表示確認
- スマートフォン等での入力時に自動的に数値キーボード（テンキー）が開くことを確認 (`inputMode="numeric"`).
